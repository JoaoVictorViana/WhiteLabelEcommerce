<?php declare( strict_types = 1 );
// phpcs:ignoreFile

namespace App;

/**
 * Add horizontal blocks bet houses
 *
 * @param array<string> $atts array of posts ids.
 */
function app_example( $atts = [] ): string {
	$content = 'example';

	return $content;
}
